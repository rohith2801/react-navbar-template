import React from 'react';
import Header from './Header';
import SideMenu from './SideMenu';

const Layout = () => {
    return <>
        <Header />
        <SideMenu />
    </>;
};

export default Layout;
