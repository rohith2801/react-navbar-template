const initialState = {};

const catalogReducer = (state: any = initialState, action: any): any => {
    switch (action) {
        default:
            return state;
    }
};

export default catalogReducer;
